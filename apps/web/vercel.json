{
  "buildCommand": "cd ../.. && npm ci --prefer-offline --no-audit && cd apps/web && npm run build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm ci --prefer-offline --no-audit",
  "framework": "nextjs"
}

