# Exemple d'environnement pour l'API (ne pas committer vos vraies clés)
# Copiez ce fichier en `.env.local` ou `.env` et remplissez les valeurs sensibles.

# URL publique de l'API (utilisée par le client)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Brevo (Sendinblue) SMTP/API
BREVO_API_KEY=votre-clé-api-brevo-ici
BREVO_SENDER_EMAIL=votre-email-ici@example.com

# JWT secret (mettre une valeur sûre en production - minimum 32 caractères)
JWT_SECRET=votre-jwt-secret-ici-minimum-32-caracteres

# Database
DATABASE_URL=postgresql://user:password@host:5432/kibei
